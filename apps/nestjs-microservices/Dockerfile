# This file is generated by Nx.
#
# Build the docker image with `npx nx docker-build @myorg/nestjs-microservices`.
# Tip: Modify "docker-build" options in project.json to change docker build args.
#
# Run the container with `docker run -p 3000:3000 -t @myorg/nestjs-microservices`.
FROM docker.io/node:lts-alpine

ENV HOST=0.0.0.0
ENV PORT=3000

WORKDIR /app

RUN addgroup --system @myorg/nestjs-microservices && \
          adduser --system -G @myorg/nestjs-microservices @myorg/nestjs-microservices

COPY apps/nestjs-microservices/dist @myorg/nestjs-microservices/
RUN chown -R @myorg/nestjs-microservices:@myorg/nestjs-microservices .

# You can remove this install step if you build with `--bundle` option.
# The bundled output will include external dependencies.
RUN npm --prefix @myorg/nestjs-microservices --omit=dev -f install

CMD [ "node", "@myorg/nestjs-microservices" ]
